# Copia este archivo como .env y rellena los valores reales
# El archivo .env NUNCA se sube a GitHub
# estas variables son genericas y funcionan unicamente de ejemplo

DB_HOST=localhost
DB_PORT=5432
DB_USER=gametracker_user
DB_PASSWORD=your_secure_password_here
DB_NAME=videogame_tracker
PORT=8080

# API Key de RAWG.io — usada solo por el script de seeding
RAWG_API_KEY=
